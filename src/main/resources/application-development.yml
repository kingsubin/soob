spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: springuser
    password: password
    url: jdbc:mysql://localhost:3306/soob_server?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: none
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
  jwt:
    secret: kingsubinkingsubinkingsubinkingsubinkingsubinkingsubinkingsubinkingsubinkingsubinkingsubin
  mail:
    host: smtp.gmail.com
    port: 587
    username: [ ]
    password: [ ]
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

settings:
  email-properties:
    verificationDuration: 1800
    verificationLink: http://localhost:8080/account/verify/
  attachment-properties:
    path: /Users/kingsubin/Desktop/Project/soob_backend/files
    profileImagePath: /profileImage/
    postImagePath: /postImage/
