server:
  port: 8081

spring:
  config:
    activate:
      on-profile: "production1"
  cache:
    type: redis
  redis:
    host: localhost
    port: 6379
  jwt:
    secret: kingsubinkingsubinkingsubinkingsubinkingsubinkingsubinkingsubinkingsubinkingsubinkingsubin
